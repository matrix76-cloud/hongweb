# 배포 전 확인 목록 (2026-08-13 작성)

앱·웹을 실제로 내보내기 전에 형이 확인해줘야 하는 것들.
코드는 다 준비돼 있고, 아래가 정해져야 올릴 수 있다.

---

## 1. 웹 — 어느 주소에 올릴지 ★가장 먼저

지금 상황이 이렇다.

| 주소 | 지금 올라가 있는 것 | 번들 |
|---|---|---|
| honglady.co.kr | **"구해줘 홍여사 · 병원동행"** (CareService로 보임, 6/17자) | `index-inIa4Dn-.js` |
| help-4902e.web.app | 위와 동일 (같은 사이트) | `index-inIa4Dn-.js` |
| help-bbcb5.web.app | "구해줘 홍여사" (내용 확인 필요) | 번들 참조 없음 |
| help-bbcb9.web.app | "구해줘 홍여사" (내용 확인 필요) | 번들 참조 없음 |

**우리가 하루 종일 고친 웹은 아직 어디에도 배포되지 않았다.**
로컬 최신 빌드는 `index-DD-7gwqQ.js` 인데 어느 사이트도 이걸 물고 있지 않다.

확인할 것
- [ ] `honglady.co.kr` 에 올라간 게 CareService 가 맞나? 맞다면 그건 어디로 옮기나
- [ ] 홍여사 웹을 `honglady.co.kr` 로 올릴 건가 (그러면 지금 것이 덮인다)
- [ ] `help-bbcb5` / `help-bbcb9` 에 있는 건 뭔가 (지워도 되는 건지)

※ firebase.json 의 hosting 설정도 손봐야 한다.
  지금은 target 없는 항목이 `public/` 폴더를 기본 사이트로 올리게 돼 있는데,
  빌드 결과물은 `build/` 다. 이대로 `firebase deploy --only hosting` 하면
  엉뚱한 게 올라간다.

---

## 2. 앱 — 현재 마켓 버전

로컬 소스는 `versionCode 1 / versionName 1.0` 인데, 마켓 버전은 이보다 높을 것이다.
**로컬 소스가 마켓에 올라간 그 소스가 맞는지**부터 확인해야 한다.
다르면, 이대로 올릴 경우 최신 기능이 빠진 앱으로 덮어쓰게 된다.

확인할 것
- [ ] 플레이 콘솔 — 패키지명이 `com.hongappios` 가 맞나
- [ ] 플레이 콘솔 — 현재 versionCode / versionName
- [ ] 앱스토어 — 현재 버전 / 빌드 번호
- [ ] 이 소스 말고 다른 곳에서 마지막 빌드를 한 적이 있나

확인되면 versionCode 를 그보다 +1 해서 올린다. (플레이는 같은 번호를 거부한다)

---

## 3. FCM — VAPID 키 (웹 푸시)

서버(Cloud Functions)는 배포 완료돼 동작을 확인했다.
웹에서 토큰을 발급받으려면 키가 하나 필요하다.

- [ ] Firebase 콘솔 → 프로젝트 설정(help-bbcb5) → 클라우드 메시징
      → 웹 푸시 인증서 → 키 쌍 생성
- [ ] 받은 값을 `.env` 에 `VITE_FCM_VAPID_KEY=...` 로 저장

키가 들어오면 리뷰 > FCM 테스트에서 [발송] 로 실제 푸시까지 확인 가능하다.
(지금도 [미리보기]로 배너 모양은 볼 수 있다)

---

## 4. 앱 서명 지문 (소셜 로그인 붙일 때)

카카오 로그인을 붙이면 지문 3벌을 전부 등록해야 한다. 하나라도 빠지면
**스토어에서 받은 앱만** 로그인이 실패한다 (개발 중에는 멀쩡해서 놓치기 쉽다).

- [ ] 디버그 키 해시
- [ ] 릴리스 키 해시
- [ ] 플레이 앱 서명 키 해시 (플레이 콘솔 → 앱 무결성)
- [ ] 위 셋을 카카오 개발자 콘솔 + Firebase SHA 지문에 등록

---

## 5. 그 외 미뤄둔 것

- [ ] Node 20 런타임이 2026-10-30 폐기 예정 → 그 전에 22 이상으로 올려야 함
- [ ] `functions/serviceAccountKey.json` 의 예전 키 폐기 (git 히스토리에 남아 있다)
- [ ] TURN 서버 — 보이스톡이 회사망 등에서 안 붙으면 그때 붙인다 (유료)
- [ ] `utility/room.js` 잔재 정리 (공간대여 폐지 후에도 17개 파일이 참조 중)

---

## 준비 완료된 것 (확인만 되면 바로 나갈 수 있음)

- 웹: 초심 복원 정리, Vite 이관, 홈·카드·내 정보·범위설정, 시드 데이터
- 서버: FCM 발송 함수 3종 배포 완료 (`onNotificationCreate` 동작 확인)
- 앱: 크래시 3건 수정, 뒤로가기, 푸시 딥링크, 권한(알림·위치·마이크), 주소 상수화
- 보이스톡: 통화 문서 + WebRTC + 통화 화면 + 푸시 연동
