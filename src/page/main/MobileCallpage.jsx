import React from "react";
import MobileCallcontainer from "../../container/main/MobileCallcontainer";

/** 보이스톡 — 전체화면이라 레이아웃(헤더·하단탭) 없이 컨테이너만 띄운다 */
const MobileCallpage = () => <MobileCallcontainer />;

export default MobileCallpage;
