# 할 일

> 판단 기준은 [CORE.md](./CORE.md). 새 항목을 넣기 전에 "①~④ 중 하나를 돕는가"를 먼저 통과시킨다.
> 마지막 정리: 2026-08-12

## 핵심 흐름 진행 상황

```
① 일 올리기 ──── 됨 (홈 카테고리 → 등록)
② 지원하기 ───── 됨 (일감 상세 → 지원 → 대화방 생성)
③ 픽 ─────────── 없음        ← 여기가 비어 있다
④ 연결(채팅) ─── 됨 (목록·대화방·안읽음)
⑤ 결제 ───────── 없음
```

③이 없으면 맡기는 사람에게 선택권이 없다. CORE 가 말하는 "우리가 서는 편"이 성립하지 않는다.

---

## 다음 순서

### 1. ③ 픽 (지원자 고르기) — 가장 급함
- 한 일감에 지원한 사람들을 한 화면에서 본다
- 그중 한 명을 고르면 그 방만 남기고 나머지에게는 마감을 알린다
- 지원 내역을 담는 자리가 아직 없다. 지금은 CHAT 문서가 그 역할을 겸하고 있어 "지원자 목록"을 뽑기 어렵다
- 일감의 `APPLY_COUNT` 가 이 기능이 생겨야 채워진다 (지금 0 이 정상)

### 2. 로그인 (소셜 3종)
- 카카오 · 구글 · 애플만. 전화번호 SMS 인증은 걷어낸다 ("로그인이 너무 어려웠다"가 실패 원인 중 하나)
- 카카오는 Cloud Function 으로 커스텀 토큰 발급 + IAM 에 Service Account Token Creator 필요
- 지금은 로그인 없이 흐름을 보려고 데모 계정을 쓰는 중이다. 로그인이 붙으면 아래 "정리할 것" 참고

### 3. ⑤ 결제
- 채팅에서 서로 합의하면 결제로 넘어가고, 그 돈이 플랫폼으로 들어온다
- `@tosspayments/tosspayments-sdk` 는 이미 의존성에 있다

---

## 채팅 — 남은 것

- **푸시 알림** — 지금은 앱을 켜고 있어야 새 대화를 안다. 통화보다 이게 먼저 효과가 크다
  (FCM 패턴은 전역 메모의 jogunBiz 구현 참고)
- **방 목록 쿼리 전환** — 지금은 CHAT 전체를 받아 내 방만 걸러낸다. 방이 쌓이면
  `PARTICIPANTS` 배열 쿼리로 바꿔야 한다 (Firestore 색인 필요). 새로 만드는 방에는 필드를 이미 심어뒀다
- **읽음 표시 두 갈래 정리** — 기존 데이터는 메시지의 `READ` 배열, 새로 넣은 건 방 문서의 `UNREAD` 맵.
  둘 중 하나로 모으는 게 낫다
- **나가기 · 신고 · 차단** — seekone 에 있는 것들. 실제 사용자가 붙기 전에 필요

---

## 보류 (결정 필요)

### 보이스톡 (앱 내 음성통화)
지금은 통화 관련 코드도 의존성도 없다. 붙일 수는 있고, 정할 게 두 가지다.

- **어디에 붙이나**
  - RN 네이티브 화면 (권장) — 웹에서 브릿지로 넘기고 통화 UI·마이크·백그라운드는 네이티브가 처리
  - WebView 안에서 직접 — Android `onPermissionRequest` + `RECORD_AUDIO`, iOS `NSMicrophoneUsageDescription` 필요.
    통화 중 백그라운드로 가면 WebView 가 죽는 문제까지 다뤄야 한다
- **무엇으로 붙이나**
  - Agora / LiveKit (권장) — TURN 포함. Agora 음성 월 10,000분 무료, 이후 1,000분당 약 $1
  - 직접 WebRTC — 시그널링은 Firestore 로 되지만 TURN 서버를 직접 운영해야 한다. 없으면 통화 실패율이 높다

순서상으로는 ③픽 · 로그인 · 푸시 다음이다. 번호 노출 없이 연락하게 하는 게 목적이라면 푸시가 먼저다.

---

## 정리할 것 (기술 부채)

- **데모 계정** — 로그인 없이 흐름을 보려고 만든 것(`DEVICEID: demo-review-device`).
  진짜 로그인이 붙으면 리뷰페이지 [시드 삭제] 로 시드 방과 함께 지운다 (`SEEDED: true` 표시가 붙어 있다)
- **진행여부 팝업** — 체크박스로 옮겨서 이제 열릴 일이 없다. 되돌릴 수 있게 남겨뒀다 (`MobileProcessFilter`)
- **위치 재설정 화면 두 개** — `/Mobilemappick`(지도 고정핀, seekone 방식)과
  `/Mobilemapreconfig`(다음 우편번호 주소 등록). 성격이 달라 둘 다 뒀는데, 하나로 합칠지 정해야 한다
- **CHAT 스키마** — DB 는 `INFO`·`CREATEDT`, 코드 일부는 `WORK_INFO`·`CREATEDAT` 를 쓰고 있었다.
  `utility/chat.js` 가 양쪽을 다 받아주게 해뒀지만, 언젠가 한쪽으로 모으는 게 맞다
- **홈 필터 로직** — 필터 상태가 팝업마다 따로 놀아서 조합이 어렵다

---

## 하지 않는 것

[CORE.md 의 영구 금지 목록](./CORE.md) 을 따른다. 공간대여 · 일자리 검색 · 커뮤니티 ·
생활정보 · 게임형 이벤트 · 레시피/운세/웹툰. 넣고 싶어지면 CORE 를 먼저 고친다.
