# 구해줘 홍여사 — 초심 (판단 기준)

> 이 문서가 이 프로젝트의 자다.
> 기능을 추가하거나 유지할지 고민되면 코드를 보기 전에 이 문서를 먼저 본다.

## 한 문장

**일 맡기는 사람이, 사람을 구한다.**

이름이 이미 정답이었다. "구해줘 홍여사"는 **일손이 필요한 사람이 하는 말**이다.
일자리를 구하는 사람의 말이 아니다.

## 핵심 흐름 — 이게 전부다

```
① 일 맡기는 사람이 일을 올린다
        ↓
② 홍여사들이 지원한다
        ↓
③ 맡기는 사람이 그중에서 픽한다
        ↓
④ 연결된다
```

이 네 단계 밖의 것은 이 앱의 일이 아니다.

## 우리가 서는 편

| | |
|---|---|
| 주인공 (앱의 주 사용자) | **일 맡기는 사람** |
| 상대 | **홍여사** — 지원하는 여성 일손 |
| 시작 화면 | 일감 구경이 아니라 **일 올리기** |
| 선택권 | **맡기는 사람에게 있다** (지원 → 픽) |
| 닮은 모델 | 숨고 (알바몬 아님) |

## 판단 기준 — 새 기능 앞에서 던지는 질문

1. 이게 **①~④ 네 단계 중 하나**를 돕는가?
2. 이게 **성사된 매칭 한 건**을 더 만들어내는가?

둘 중 하나라도 아니오면 만들지 않는다. 예외 없다.

## 지표

세지 않는 것은 관리되지 않는다. 이 앱이 보는 숫자는 하나다.

**성사된 매칭 건수.**

체류시간·가입자수·기능수는 보지 않는다.

## 실패에서 배운 것 (2024~2026)

1. **핵심을 못 찾았다.** 그래서 핵심이 비어 있는 줄도 몰랐다.
   - 실제로 `_handleSupport = () => {}` — ②지원하기 버튼이 빈 함수인 채로 방치됐다.
   - ③픽 기능은 아예 없었다. 지원 내역을 담는 컬렉션조차 없었다.
   - 그 사이 서비스 레이어는 49개까지 늘었다. 코드의 72%는 앱에 연결조차 안 된 죽은 코드였다.
2. **축이 처음부터 두 개였다.** 1일차 설계도의 하단탭에 이미 "공간대여"가 있었다.
   축이 둘인 게 허용되면 셋, 넷도 허용된다. 레시피·게임·출석체크까지 가는 길이 그때 열렸다.
3. **어느 편에 설지 안 정했다.** 이름은 일 맡기는 사람을 부르는데,
   일자리 검색·이력서·워크넷 수집 같은 구직자 편 기능을 같이 넣었다.
   양쪽 다 "내 앱 아닌데" 하고 나간다.
4. **불안하면 기능을 붙이게 된다.** 기준이 없을 때 사람이 하는 행동이다.
   기능 추가는 대개 자신 없음의 표현이지 전략이 아니다.

## 하지 않는 것 (영구 금지 목록)

- 공간·물건 대여 — 사람이 아니다
- 일자리 검색 / 이력서 / 외부 채용공고 수집(워크넷) — 구직자 편이다
- 커뮤니티·게시판 — 체류시간용이다
- 생활정보(관광·병원·약국·캠핑·공연·축제) — 매칭을 만들지 않는다
- 출석체크·룰렛·사다리·레이스 등 게임형 이벤트 — 매칭을 만들지 않는다
- 레시피·냉장고·물마시기·운세·웹툰 — 설명이 필요 없다

## 작업 규칙

- **화면 수 상한을 먼저 정한다.** 늘리려면 이 문서를 먼저 고친다.
- **git을 실제로 쓴다.** 파일 복사로 버전 관리하지 않는다.
  `xxx copy.js`가 등장하는 순간 그 프로젝트는 이미 관리 밖이다.
- **키는 레포에 두지 않는다.** 서비스 계정 키·API 키 전부.
- **rules 파일이 곧 실제 보안 규칙이다.** 콘솔에서 따로 열어두지 않는다.
